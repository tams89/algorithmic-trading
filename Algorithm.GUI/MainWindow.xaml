<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" x:Class="Algorithm.GUI.MainWindow"
        Title="Realtime Feed" Height="500" Width="900">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Symbol:" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Symbol, UpdateSourceTrigger=Explicit}"/>
        <Button Grid.Row="0" Grid.Column="2" Content="Refresh" Command="{Binding UpdateCommand}"/>
      
    </Grid>
</Window>